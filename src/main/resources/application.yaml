spring:
  datasource:
    url: jdbc:mysql://localhost:3306/skooly_dev
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    #    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

logging:
  level:
    root: DEBUG
    org.springframework: DEBUG